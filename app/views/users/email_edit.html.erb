<% provide(:title, "メールアドレス編集") %>
<h1>メールアドレス編集</h1>
<%= form_with model: @user, url: email_update_path do |f| %>
<%= render "shared/error", obj: @user %>
  <div>
    <%= f.label :メールアドレス %><br>
    <%= f.text_field :email %>
  </div>
  <div>
    <%= f.submit "更新" %>
  </div>
<% end %>
<%= link_to "戻る", :back %><br>
